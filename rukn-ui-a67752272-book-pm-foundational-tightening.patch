diff --git a/books/pm_schema.py b/books/pm_schema.py
--- a/books/pm_schema.py
+++ b/books/pm_schema.py
@@ -26,43 +26,35 @@ def book_page_marker_node(
         'nonAuthor': non_author,
         'services': list(services or []),
     }
-    _validate_book_pm_contract_attrs('nodes', 'bookPageMarker', attrs)
+    _validate_book_pm_contract_attrs(BOOK_PM_NODE_ATTR_SPECS['bookPageMarker'], attrs)
     return {'type': 'bookPageMarker', 'attrs': attrs}
 
 
 def canonicalize_book_pm_text(text: str | None) -> str:
-    return live_book_pm_envelope(parse_book_pm_envelope(text)['doc'])
+    return _book_pm_envelope(parse_book_pm_envelope(text)['doc'])
 
 
 def _same_json_value(left: Any, right: Any) -> bool:
-    if type(left) is not type(right):
-        return False
-    if isinstance(left, dict):
-        return left.keys() == right.keys() and all(_same_json_value(left[key], right[key]) for key in left)
-    if isinstance(left, list):
-        return len(left) == len(right) and all(_same_json_value(a, b) for a, b in zip(left, right, strict=True))
-    return left == right
+    return orjson.dumps(left, option=orjson.OPT_SORT_KEYS) == orjson.dumps(
+        right, option=orjson.OPT_SORT_KEYS
+    )
 
 
 def legacy_volume_text_from_book_pm_doc(doc_json: dict[str, Any]) -> str:
-    doc = validate_live_book_pm_doc(doc_json)
     pages: list[str] = []
-    for marker, blocks in iter_book_pm_page_segments(doc):
+    for marker, blocks in iter_book_pm_page_segments(doc_json):
         pages.append(str((marker, blocks)))
     return ''.join(pages)
 
 
 def _validate_book_pm_contract_doc(doc: dict[str, Any]) -> None:
-    for invariant in BOOK_PM_DOCUMENT_INVARIANTS:
-        if invariant.get('kind') != 'unique_node_attr':
-            raise RuntimeError('unknown_book_pm_document_invariant')
     unique_values = [set() for _ in BOOK_PM_DOCUMENT_INVARIANTS]
 
     for node in _iter_book_pm_nodes(doc):
         node_type = node['type']
-        _validate_book_pm_contract_attrs('nodes', node_type, node.get('attrs') or {})
+        _validate_book_pm_contract_attrs(BOOK_PM_NODE_ATTR_SPECS[node_type], node.get('attrs') or {})
         for mark in node.get('marks') or ():
-            _validate_book_pm_contract_attrs('marks', mark['type'], mark.get('attrs') or {})
+            _validate_book_pm_contract_attrs(BOOK_PM_MARK_ATTR_SPECS[mark['type']], mark.get('attrs') or {})
         for invariant, seen in zip(BOOK_PM_DOCUMENT_INVARIANTS, unique_values, strict=True):
             if node_type != invariant['node']:
                 continue
@@ -72,8 +64,7 @@ def _validate_book_pm_contract_doc(doc: dict[str, Any]) -> None:
             seen.add(value)
 
 
-def _validate_book_pm_contract_attrs(owner: str, name: str, attrs: dict[str, Any]) -> None:
-    specs = (BOOK_PM_NODE_ATTR_SPECS if owner == 'nodes' else BOOK_PM_MARK_ATTR_SPECS).get(name, {})
+def _validate_book_pm_contract_attrs(specs: dict[str, Any], attrs: dict[str, Any]) -> None:
     if set(attrs) != set(specs):
         raise ValueError('noncanonical_pm_doc')
 
@@ -83,11 +74,7 @@ def _clean_page_raw(value: str) -> str:
 
 
 def book_printed_page_number(value: Any) -> int | None:
-    return _parse_int(value if isinstance(value, str) else None)
-
-
-def _parse_int(value: str | None) -> int | None:
-    cleaned = _clean_page_raw(value or '')
+    cleaned = _clean_page_raw(value if isinstance(value, str) else '')
     if not re.fullmatch(r'[0-9]+', cleaned):
         return None
     parsed = int(cleaned)
diff --git a/client/editor/books/book-pm-model.js b/client/editor/books/book-pm-model.js
--- a/client/editor/books/book-pm-model.js
+++ b/client/editor/books/book-pm-model.js
@@ -9,18 +9,13 @@ export function assert_book_pm_document_contract(doc) {
 }
 
 function assert_book_pm_contract(root, invariants) {
-    const unique_values = invariants.map(invariant => {
-        if (invariant.kind !== 'unique_node_attr') {
-            throw new Error('unknown_book_pm_document_invariant')
-        }
-        return new Set()
-    })
+    const unique_values = invariants.map(() => new Set())
     const pending = [root]
     while (pending.length) {
         const node = pending.pop()
-        assert_book_pm_attrs('nodes', node.type.name, node.attrs)
+        assert_book_pm_attrs(wire_identity.nodes[node.type.name]?.attrs || {}, node.attrs)
         for (const mark of node.marks) {
-            assert_book_pm_attrs('marks', mark.type.name, mark.attrs)
+            assert_book_pm_attrs(wire_identity.marks[mark.type.name]?.attrs || {}, mark.attrs)
         }
         invariants.forEach((invariant, index) => {
             if (node.type.name !== invariant.node) return
@@ -34,8 +29,7 @@ function assert_book_pm_contract(root, invariants) {
     }
 }
 
-function assert_book_pm_attrs(owner, name, attrs) {
-    const specs = wire_identity[owner]?.[name]?.attrs || {}
+function assert_book_pm_attrs(specs, attrs) {
     if (Object.keys(attrs).length !== Object.keys(specs).length) {
         throw new RangeError('noncanonical_pm_doc')
     }
@@ -49,26 +43,15 @@ function book_url_path(value) {
     return value.split(/[?#]/, 1)[0]
 }
 
-export function book_image_source_from_dom(src_value, declared_hash = null) {
+export function book_image_source_from_dom(src_value) {
     const src = normalize_book_url(src_value, 'media_url')
-    const attr_hash = typeof declared_hash === 'string' && BOOK_ASSET_HASH_RE.test(declared_hash)
-        ? declared_hash
-        : null
-    if (declared_hash && !attr_hash) return null
-    const url_hash = String(src || '').match(BOOK_ASSET_URL_RE)?.[1] || null
-    if (src && BOOK_ASSET_PATH_RE.test(book_url_path(src)) && !url_hash) return null
-    if (attr_hash && src && url_hash !== attr_hash) return null
-    const hash = attr_hash || url_hash
+    const hash = String(src || '').match(BOOK_ASSET_URL_RE)?.[1] || null
+    if (src && BOOK_ASSET_PATH_RE.test(book_url_path(src)) && !hash) return null
     if (hash) return {kind: 'asset', hash}
     return src ? {kind: 'url', url: src} : null
 }
 
 export function book_image_dom_attrs(source) {
-    if (source?.kind === 'asset') {
-        return {
-            src: `/api/books/book-assets/${source.hash}/`,
-            'data-book-asset': source.hash,
-        }
-    }
+    if (source?.kind === 'asset') return {src: `/api/books/book-assets/${source.hash}/`}
     return source?.kind === 'url' ? {src: source.url} : {}
 }
diff --git a/client/editor/books/book-pm-model.test.js b/client/editor/books/book-pm-model.test.js
--- a/client/editor/books/book-pm-model.test.js
+++ b/client/editor/books/book-pm-model.test.js
@@ -8,6 +8,7 @@ import {
     assert_book_pm_document_contract,
     assert_book_pm_node_contract,
     book_pm_attr_specs,
+    book_image_dom_attrs,
     book_pm_blocked_response_facts,
     book_image_source_from_dom,
     book_link_attrs_from_dom,
@@ -29,6 +30,9 @@ test('Book PM constructors produce only canonical attribute values', () => {
         kind: 'asset',
         hash: 'a'.repeat(40),
     })
+    assert.deepEqual(book_image_dom_attrs({kind: 'asset', hash: 'a'.repeat(40)}), {
+        src: `/api/books/book-assets/${'a'.repeat(40)}/`,
+    })
     assert.equal(
         book_image_source_from_dom(
             'https://example.com/api/books/book-assets/' + 'a'.repeat(40) + '/',
diff --git a/client/editor/books/book-pm-schema.js b/client/editor/books/book-pm-schema.js
--- a/client/editor/books/book-pm-schema.js
+++ b/client/editor/books/book-pm-schema.js
@@ -8,10 +8,7 @@ function define_book_image() {
             {
                 tag: 'img',
                 getAttrs: dom => {
-                    const source = book_image_source_from_dom(
-                        dom.getAttribute('src'),
-                        dom.getAttribute('data-book-asset'),
-                    )
+                    const source = book_image_source_from_dom(dom.getAttribute('src'))
                     if (!source) return false
                     return {
                         source,
